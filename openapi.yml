openapi: 3.1.0
info:
  version: 0.1.0
  title: g
paths:
  /account/register:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
              required:
                - name
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  accountId:
                    type: integer
                required:
                  - name
  /account/{accountId}/balance:
    get:
      parameters:
        - in: path
          name: accountId
          schema:
            type: integer
          required: true
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  balance:
                    type: string # balance is decimal
